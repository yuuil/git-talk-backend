type Mutation {
  createMessage(
    chatId: String!,
    chatKey: String!,
    userId: String!,
    plainText: String!,
    actions: [Block!],
    buttons: [Button!],
    files: [File!],
    submit: Submit
  ): Boolean!
}