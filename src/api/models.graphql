type User {
  id: ID!
  name: String
  avatarUrl: String
  createdAt: String!
}

type Chat {
  id: ID!
  userId: String!
  state: String!
  lastMessage: Message!
  askedAt: String
  closedAt: String
  createdAt: String!
}

type Message {
  id: ID!
  chatId: String!
  chatKey: String!
  plainText: String!
  actions: [Action!]
  blocks: [Block!]
  buttons: [Button!]
  files: [File!]
  createdAt: String!
}

type Block {
  id: ID!
  type: String!
  value: String!
}

type Submit {
  id: String!
  key: String!
}

type Action {
  id: ID!
  key: String!
  text: String!
  createdAt: String!
}

type Button {
  id: ID!
  title: String!
  theme: String
  url: String!
  createdAt: String!
}

type File {
  id: ID!;
  type: String!
  name: String!
  size: Int!
  animated: Boolean!
  contentType: String!
  width: Int
  height: Int
  createdAt: String!
}
